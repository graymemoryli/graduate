<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--        namespace命名空间，对应到相应的接口-->
<mapper namespace="com.hwadee.train.mapper.UserMapper">



<!--    <insert id="createUserAndReturnId" parameterType="User" useGeneratedKeys="true" keyProperty="id">-->
<!--        insert into user(name,pwd,amount,create_time) values(#{name},#{pwd},#{amount},#{createTime})-->
<!--    </insert>-->


</mapper>